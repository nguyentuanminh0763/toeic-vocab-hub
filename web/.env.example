# Sao chép thành .env.local cho dev: cp .env.example .env.local
# Cho production (Vercel/Netlify): set biến này trong dashboard của platform

# URL của backend API — không có trailing slash
NEXT_PUBLIC_API_URL=http://localhost:3000
